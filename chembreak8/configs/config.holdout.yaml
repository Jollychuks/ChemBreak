_base_: config.development.yaml
run:
  phase: holdout
  seed: 7112026
experiment:
  task_count: 400
  verification_mode: strict
  negative_audit_rate: 1.0
policy:
  mode: frozen
  artifact_path: /content/chembreak8_storage/policies/frozen_policy.json
