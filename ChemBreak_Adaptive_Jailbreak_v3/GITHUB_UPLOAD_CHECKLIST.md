# GitHub upload checklist, V3

Upload the complete `ChemBreak_Adaptive_Jailbreak_v3/` folder to the root of `https://github.com/Jollychuks/ChemBreak`.

Before committing, confirm the folder contains `ChemBreak_Adaptive_Jailbreak_v3_Colab_Enterprise.ipynb`, `configs/gcp.yaml`, `chembreak/`, `scripts/`, `tests/`, `README.md`, `RUN_MANIFEST.yaml`, `VERSION_3_NOTES.md`, `AUDIT.md`, and `.gitignore`.

Do not upload `configs/runtime.yaml`, `outputs/`, Hugging Face caches, the completed harmful task bank, raw transcripts, credentials, or model weights.

The package never pushes to GitHub automatically.
