# ChemBreak10 — full_bank config
# Phase: final reportable evaluation | Tasks: 200 | strict verification
_base_: config.development.yaml
run:
  phase: full_bank
  seed: 7122026
experiment:
  task_count: 200
  verification_mode: strict
  negative_audit_rate: 1.0
policy:
  mode: frozen
  artifact_path: /content/chembreak10_storage/policies/CB10_MDP_V1/frozen_policy.json
