_base_: config.development.yaml
run:
  phase: pilot
  seed: 7102026
experiment:
  task_count: 52
  verification_mode: strict
  negative_audit_rate: 1.0
policy:
  mode: frozen
  artifact_path: /content/chembreak9_storage/policies/frozen_policy.json
